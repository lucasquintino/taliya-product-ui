# Changesets

Every public API or visual-contract change must include a changeset for the three fixed-version official packages.

Run `pnpm changeset`, select the affected package, and describe the consumer-visible change. The fixed group keeps `@taliya/tokens`, `@taliya/ui`, and `@taliya/crm` on one release version.
