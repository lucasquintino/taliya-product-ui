# Component Architecture Audit

Generated by `pnpm components:audit:update`.

## Storybook architecture

- Stories scanned: 276
- Invalid titles: 0
- Legacy `CRM / Batch` titles: 0
- Removed obsolete gallery story: `apps/docs/src/stories/CrmBatch9Patterns.stories.tsx`

Accepted namespaces:

- `Foundations / Tokens`
- `Primitives / UI`
- `CRM / Shell / Components`
- `CRM / Image Coverage`
- `CRM / <official family> / <component>`

## CRM primitive reuse

- CRM function components scanned: 192
- Components with native controls: 30
- Justified compound-root controls: 30
- Controls requiring refactor: 0
- Controls requiring a missing primitive: 0
- Native control totals:
  - button: 34
  - anchor: 1

Native controls are not automatically wrong. They are allowed only when the native element is the official root anatomy of the component, such as an interactive card, row, calendar day, or browser toolbar control. They are debt when they recreate an existing primitive control such as `Button`, `IconButton`, `Input`, `Checkbox`, `Select`, `Tabs`, `Chip`, `FilterChip`, `ComposerInput`, or `DataTable`.

| Component | Native controls | Official primitives already used | Status | Required action |
| --- | --- | --- | --- | --- |
| `CrmBrowserToolbarButton` | button:1 | Icon | Justificado | Toolbar browser control: native button is the component root; icon rendering is delegated to `Icon`. |
| `CrmShellAvatar` | button:1 | Avatar | Justificado | Account/avatar trigger: native button is the component root; avatar visual is delegated to `Avatar`. |
| `CrmOperationalRow` | button:1 | Chip, Icon, StatusDot | Justificado | Operational row: native button is the full compound row root; icon, status, and chip visuals are delegated to official primitives. |
| `ActivityFeed` | button:1 | Icon, IconButton | Justificado | Timeline event root/action: native button represents an interactive event row, not a standalone button primitive. |
| `OperationActivityTable` | button:1 | Avatar, Button, Chip | Justificado | Operation activity table row: native button is the full row root for row-open behavior; header/action controls use official primitives. |
| `KanbanCard` | button:2 | Card, Chip, Icon, IconButton | Justificado | Interactive kanban card anatomy: native buttons are card-level roots for draggable/selectable card affordances. |
| `MiniCalendar` | button:1 | IconButton | Justificado | Calendar day cell anatomy: native button is the semantic day cell root. |
| `SetupShell` | button:1 | Avatar, Button, Chip, IconButton, ProgressBar, TaliyaLogo | Justificado | Shell account selector root: native button wraps an avatar/account compound control. |
| `SetupChoiceCard` | button:1 | Icon | Justificado | Selectable setup option card: native button is the full card root. |
| `SetupImportSourceCard` | button:1 | Icon | Justificado | Selectable import-source card: native button is the full card root. |
| `SetupReviewPanel` | button:1 | Button, Checkbox, Chip, Icon | Justificado | Review-area selector: native button is the compound published-area root; checkbox state uses `Checkbox`. |
| `AccessFooterLinks` | anchor:1 | - | Justificado | Footer legal/navigation link: native anchor is required semantic navigation. |
| `IntegrationStatusRow` | button:1 | Icon | Justificado | Provider row action: native button is the interactive status row root. |
| `CommentThread` | button:1 | Avatar, Button, Card | Justificado | Comment row action: native button is the interactive comment/root affordance. |
| `TaskDrawer` | button:1 | Avatar, Badge, Button, Icon | Justificado | Checklist row action: native button is a row-level task completion root. |
| `ReplacementDrawer` | button:1 | Button, Chip, Icon, IconButton | Justificado | Replacement option row: native button is the selectable option-card root. |
| `WeeklyHoursGrid` | button:3 | Icon | Justificado | Schedule grid controls: native buttons are day/slot cells with domain-specific grid anatomy. |
| `RoleCard` | button:1 | Avatar, Chip | Justificado | Selectable role card: native button is the compound card root. |
| `PaymentMethodRow` | button:1 | Icon | Justificado | Selectable payment method row: native button is the compound row root. |
| `UsageOriginRow` | button:1 | - | Justificado | Usage origin row: native button is the expandable/inspectable row root. |
| `ModeCard` | button:1 | Chip, Icon | Justificado | Selectable agent mode card: native button is the compound card root. |
| `PreflightChecklist` | button:2 | Panel | Justificado | Preflight checklist item/action: native buttons are checklist-row roots where the whole row is interactive. |
| `ScenarioList` | button:1 | Icon, Panel | Justificado | Scenario list item: native button is the selectable row root. |
| `FinancePriorityPanel` | button:1 | Icon, Panel | Justificado | Finance priority row: native button is the full source-derived row root; icon and panel visuals are delegated to official primitives. |
| `PaymentCaseCard` | button:2 | Badge, Card, Icon, IconButton | Justificado | Payment case card: native buttons are card-level domain roots; icon actions use `IconButton`. |
| `PipelineCard` | button:1 | Card, Chip, Icon, IconButton | Justificado | Pipeline card: native button is the selectable card root; icon actions use `IconButton`. |
| `TrialClassCard` | button:1 | Avatar, Chip, Icon | Justificado | Trial class card: native button is the selectable row/card root. |
| `EnrollmentChecklist` | button:1 | Icon, List | Justificado | Enrollment checklist row: native button is the completion/selection row root. |
| `RiskCard` | button:1 | Avatar, Card, Chip, Icon | Justificado | Retention risk card: native button is the card-level action root. |
| `PageQuickFilters` | button:1 | Badge, Icon | Justificado | Quick filter row: native button is the full selectable filter row root; icon and count visuals are delegated to official primitives. |

## Closure rule

This debt is closed only when every `Refatorar` row is converted to official primitives, and every `Primitive faltante` row is resolved by extracting the missing primitive. `Justificado` rows are allowed only because their action cell documents the deliberate component anatomy contract.
