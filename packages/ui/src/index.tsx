/**
 * Public compatibility facade. Owned implementation modules are intentionally
 * hidden behind this stable entry point so consumer imports remain unchanged.
 */
export * from "./internal-ui-runtime.js";
